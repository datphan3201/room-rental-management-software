PORT=4000
NODE_ENV=development
# Kept for future MongoDB swap; current build uses local repository storage.
MONGODB_URI=
JWT_SECRET=room-rental-management-local-secret
JWT_EXPIRES_IN=7d
CLIENT_ORIGIN=http://127.0.0.1:5173
